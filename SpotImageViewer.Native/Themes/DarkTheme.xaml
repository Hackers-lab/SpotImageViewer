<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- =========================================================================
         EXACT CSS COLOR TOKENS (app.css v20.56 Industrial Studio UI)
         ========================================================================= -->
    <Color x:Key="ColorAppBg">#141414</Color>
    <Color x:Key="ColorSurface0">#181818</Color>
    <Color x:Key="ColorSurface1">#1f1f1f</Color>
    <Color x:Key="ColorSurface2">#252525</Color>
    <Color x:Key="ColorSurface3">#2b2b2b</Color>
    <Color x:Key="ColorSurfaceHover">#303030</Color>
    <Color x:Key="ColorBorder">#18FFFFFF</Color>       <!-- rgba(255,255,255,0.085) -->
    <Color x:Key="ColorBorderStrong">#26FFFFFF</Color> <!-- rgba(255,255,255,0.13) -->
    <Color x:Key="ColorTextPrimary">#f2f2f2</Color>
    <Color x:Key="ColorTextSecondary">#c0c0c0</Color>
    <Color x:Key="ColorTextMuted">#888888</Color>
    <Color x:Key="ColorCanvas">#101010</Color>

    <Color x:Key="ColorAccentSky">#31a8ff</Color>
    <Color x:Key="ColorAccentSkyDark">#168fe0</Color>
    <Color x:Key="ColorAccentPurple">#7c5cff</Color>
    <Color x:Key="ColorAccentEmerald">#35c98a</Color>
    <Color x:Key="ColorAccentAmber">#f5b84b</Color>
    <Color x:Key="ColorAccentRose">#ff5f70</Color>
    <Color x:Key="ColorAccentCyan">#06b6d4</Color>
    <Color x:Key="ColorAccentIndigo">#818cf8</Color>

    <!-- SolidColorBrushes -->
    <SolidColorBrush x:Key="BrushAppBg" Color="{StaticResource ColorAppBg}" />
    <SolidColorBrush x:Key="BrushSurface0" Color="{StaticResource ColorSurface0}" />
    <SolidColorBrush x:Key="BrushSurface1" Color="{StaticResource ColorSurface1}" />
    <SolidColorBrush x:Key="BrushSurface2" Color="{StaticResource ColorSurface2}" />
    <SolidColorBrush x:Key="BrushSurface3" Color="{StaticResource ColorSurface3}" />
    <SolidColorBrush x:Key="BrushSurfaceHover" Color="{StaticResource ColorSurfaceHover}" />
    <SolidColorBrush x:Key="BrushBorder" Color="{StaticResource ColorBorder}" />
    <SolidColorBrush x:Key="BrushBorderStrong" Color="{StaticResource ColorBorderStrong}" />
    <SolidColorBrush x:Key="BrushTextPrimary" Color="{StaticResource ColorTextPrimary}" />
    <SolidColorBrush x:Key="BrushTextSecondary" Color="{StaticResource ColorTextSecondary}" />
    <SolidColorBrush x:Key="BrushTextMuted" Color="{StaticResource ColorTextMuted}" />
    <SolidColorBrush x:Key="BrushCanvas" Color="{StaticResource ColorCanvas}" />

    <SolidColorBrush x:Key="BrushAccentSky" Color="{StaticResource ColorAccentSky}" />
    <SolidColorBrush x:Key="BrushAccentSkyDark" Color="{StaticResource ColorAccentSkyDark}" />
    <SolidColorBrush x:Key="BrushAccentPurple" Color="{StaticResource ColorAccentPurple}" />
    <SolidColorBrush x:Key="BrushAccentEmerald" Color="{StaticResource ColorAccentEmerald}" />
    <SolidColorBrush x:Key="BrushAccentAmber" Color="{StaticResource ColorAccentAmber}" />
    <SolidColorBrush x:Key="BrushAccentRose" Color="{StaticResource ColorAccentRose}" />
    <SolidColorBrush x:Key="BrushAccentCyan" Color="{StaticResource ColorAccentCyan}" />
    <SolidColorBrush x:Key="BrushAccentIndigo" Color="{StaticResource ColorAccentIndigo}" />

    <!-- Typography -->
    <FontFamily x:Key="AppFontFamily">Segoe UI Variable, Segoe UI, Inter, Arial, sans-serif</FontFamily>

    <!-- Base Control Styles -->
    <Style TargetType="{x:Type TextBlock}">
        <Setter Property="FontFamily" Value="{StaticResource AppFontFamily}" />
        <Setter Property="Foreground" Value="{StaticResource BrushTextPrimary}" />
    </Style>

    <!-- =========================================================================
         LUCIDE VECTOR ICON GEOMETRIES (StreamGeometry 24x24 viewBox)
         ========================================================================= -->
    <!-- panel-left -->
    <Geometry x:Key="GeoPanelLeft">M3,3 H21 V21 H3 Z M9,3 V21</Geometry>
    <!-- search -->
    <Geometry x:Key="GeoSearch">M21,21 L16.7,16.7 M11,18 A7,7 0 1,0 11,4 A7,7 0 0,0 11,18 Z</Geometry>
    <!-- image -->
    <Geometry x:Key="GeoImage">M5,3 H19 A2,2 0 0,1 21,5 V19 A2,2 0 0,1 19,21 H5 A2,2 0 0,1 3,19 V5 A2,2 0 0,1 5,3 Z M8.5,10 A1.5,1.5 0 1,0 8.5,7 A1.5,1.5 0 0,0 8.5,10 Z M21,15 L15.5,9.5 L12,13 L8.5,9.5 L3,15</Geometry>
    <!-- zap -->
    <Geometry x:Key="GeoZap">M13,2 L3,14 H12 L11,22 L21,10 H12 Z</Geometry>
    <!-- scale -->
    <Geometry x:Key="GeoScale">M16,16 L19,8 L22,16 M2,16 L5,8 L8,16 M7,21 H17 M12,3 V21 M3,7 H5 C7,7 10,6 12,5 C14,6 17,7 19,7 H21</Geometry>
    <!-- file-spreadsheet -->
    <Geometry x:Key="GeoFileSpreadsheet">M15,2 H6 A2,2 0 0,0 4,4 V20 A2,2 0 0,0 6,22 H18 A2,2 0 0,0 20,20 V7 Z M14,2 V7 H19 M8,13 H12 M14,13 H16 M8,17 H12 M14,17 H16</Geometry>
    <!-- sparkles -->
    <Geometry x:Key="GeoSparkles">M12,3 L10.1,8.8 A2,2 0 0,1 8.8,10.1 L3,12 L8.8,13.9 A2,2 0 0,1 10.1,15.2 L12,21 L13.9,15.2 A2,2 0 0,1 15.2,13.9 L21,12 L15.2,10.1 A2,2 0 0,1 13.9,8.8 Z</Geometry>
    <!-- file-check-2 -->
    <Geometry x:Key="GeoFileCheck">M15,2 H6 A2,2 0 0,0 4,4 V20 A2,2 0 0,0 6,22 H18 A2,2 0 0,0 20,20 V7 Z M14,2 V7 H19 M9,15 L11,17 L15,13</Geometry>
    <!-- receipt -->
    <Geometry x:Key="GeoReceipt">M4,2 V22 L6,21 L8,22 L10,21 L12,22 L14,21 L16,22 L18,21 L20,22 V2 L18,3 L16,2 L14,3 L12,2 L10,3 L8,2 L6,3 Z M8,7 H16 M8,11 H16 M8,15 H13</Geometry>
    <!-- settings -->
    <Geometry x:Key="GeoSettings">M12.22,2 H11.78 A2,2 0 0,0 9.78,4 V4.18 A2,2 0 0,1 8.78,5.91 L8.35,6.16 A2,2 0 0,1 6.35,6.16 L6.2,6.08 A2,2 0 0,0 3.47,6.81 L3.25,7.19 A2,2 0 0,0 3.98,9.92 L4.13,10.02 A2,2 0 0,1 5.13,11.74 V12.25 A2,2 0 0,1 4.13,13.99 L3.98,14.08 A2,2 0 0,0 3.25,16.81 L3.47,17.19 A2,2 0 0,0 6.2,17.92 L6.35,17.84 A2,2 0 0,1 8.35,17.84 L8.78,18.09 A2,2 0 0,1 9.78,19.82 V20 A2,2 0 0,0 11.78,22 H12.22 A2,2 0 0,0 14.22,20 V19.82 A2,2 0 0,1 15.22,18.09 L15.65,17.84 A2,2 0 0,1 17.65,17.84 L17.8,17.92 A2,2 0 0,0 20.53,17.19 L20.75,16.81 A2,2 0 0,0 20.02,14.08 L19.87,13.98 A2,2 0 0,1 18.87,12.26 V11.76 A2,2 0 0,1 19.87,10.02 L20.02,9.93 A2,2 0 0,0 20.75,7.2 L20.53,6.82 A2,2 0 0,0 17.8,6.09 L17.65,6.17 A2,2 0 0,1 15.65,6.17 L15.22,5.92 A2,2 0 0,1 14.22,4.19 V4 A2,2 0 0,0 12.22,2 Z M12,15 A3,3 0 1,0 12,9 A3,3 0 0,0 12,15 Z</Geometry>
    <!-- folder -->
    <Geometry x:Key="GeoFolder">M4,20 H20 A2,2 0 0,0 22,18 V8 A2,2 0 0,0 20,6 H12.07 A2,2 0 0,1 10.41,5.1 L9.59,3.9 A2,2 0 0,0 7.93,3 H4 A2,2 0 0,0 2,5 V18 A2,2 0 0,0 4,20 Z</Geometry>
    <!-- refresh-cw -->
    <Geometry x:Key="GeoRefresh">M21,12 A9,9 0 0,0 12,3 A9.75,9.75 0 0,0 5.26,5.74 L3,8 M3,3 V8 H8 M3,12 A9,9 0 0,0 12,21 A9.75,9.75 0 0,0 18.74,18.26 L21,16 M21,21 V16 H16</Geometry>
    <!-- plus -->
    <Geometry x:Key="GeoPlus">M12,5 V19 M5,12 H19</Geometry>
    <!-- x / close -->
    <Geometry x:Key="GeoClose">M18,6 L6,18 M6,6 L18,18</Geometry>
    <!-- chevron-left -->
    <Geometry x:Key="GeoChevronLeft">M15,18 L9,12 L15,6</Geometry>
    <!-- chevron-right -->
    <Geometry x:Key="GeoChevronRight">M9,18 L15,12 L9,6</Geometry>
    <!-- chevron-down -->
    <Geometry x:Key="GeoChevronDown">M6,9 L12,15 L18,9</Geometry>
    <!-- chevrons-right -->
    <Geometry x:Key="GeoChevronsRight">M13,17 L18,12 L13,7 M6,17 L11,12 L6,7</Geometry>
    <!-- zoom-in -->
    <Geometry x:Key="GeoZoomIn">M21,21 L16.7,16.7 M11,18 A7,7 0 1,0 11,4 A7,7 0 0,0 11,18 Z M11,8 V14 M8,11 H14</Geometry>
    <!-- zoom-out -->
    <Geometry x:Key="GeoZoomOut">M21,21 L16.7,16.7 M11,18 A7,7 0 1,0 11,4 A7,7 0 0,0 11,18 Z M8,11 H14</Geometry>
    <!-- maximize / fit -->
    <Geometry x:Key="GeoMaximize">M8,3 H5 A2,2 0 0,0 3,5 V8 M21,8 V5 A2,2 0 0,0 19,3 H16 M16,21 H19 A2,2 0 0,0 21,19 V16 M3,16 V19 A2,2 0 0,0 5,21 H8</Geometry>
    <!-- rotate-cw -->
    <Geometry x:Key="GeoRotateCw">M21,12 A9,9 0 1,1 12,3 A9,9 0 0,1 18.72,5.24 L21,8 M21,3 V8 H16</Geometry>
    <!-- printer -->
    <Geometry x:Key="GeoPrinter">M6,9 V2 H18 V9 M6,18 H4 A2,2 0 0,1 2,16 V11 A2,2 0 0,1 4,9 H20 A2,2 0 0,1 22,11 V16 A2,2 0 0,1 20,18 H18 M6,14 H18 V22 H6 Z</Geometry>
    <!-- download -->
    <Geometry x:Key="GeoDownload">M21,15 V19 A2,2 0 0,1 19,21 H5 A2,2 0 0,1 3,19 V15 M7,10 L12,15 L17,10 M12,15 V3</Geometry>
    <!-- copy -->
    <Geometry x:Key="GeoCopy">M16,4 H18 A2,2 0 0,1 20,6 V20 A2,2 0 0,1 18,22 H6 A2,2 0 0,1 4,20 V6 A2,2 0 0,1 6,4 H8 M9,2 H15 A1,1 0 0,1 16,3 V5 A1,1 0 0,1 15,6 H9 A1,1 0 0,1 8,5 V3 A1,1 0 0,1 9,2 Z</Geometry>
    <!-- copy-check -->
    <Geometry x:Key="GeoCopyCheck">M16,4 H18 A2,2 0 0,1 20,6 V20 A2,2 0 0,1 18,22 H6 A2,2 0 0,1 4,20 V6 A2,2 0 0,1 6,4 H8 M9,2 H15 A1,1 0 0,1 16,3 V5 A1,1 0 0,1 15,6 H9 A1,1 0 0,1 8,5 V3 A1,1 0 0,1 9,2 Z M9,14 L11,16 L15,12</Geometry>
    <!-- user -->
    <Geometry x:Key="GeoUser">M19,21 V19 A4,4 0 0,0 15,15 H9 A4,4 0 0,0 5,19 V21 M12,11 A4,4 0 1,0 12,3 A4,4 0 0,0 12,11 Z</Geometry>
    <!-- user-check -->
    <Geometry x:Key="GeoUserCheck">M16,21 V19 A4,4 0 0,0 12,15 H6 A4,4 0 0,0 2,19 V21 M9,11 A4,4 0 1,0 9,3 A4,4 0 0,0 9,11 Z M16,11 L18,13 L22,9</Geometry>
    <!-- calendar -->
    <Geometry x:Key="GeoCalendar">M8,2 V6 M16,2 V6 M3,10 H21 M5,4 H19 A2,2 0 0,1 21,6 V20 A2,2 0 0,1 19,22 H5 A2,2 0 0,1 3,20 V6 A2,2 0 0,1 5,4 Z</Geometry>
    <!-- phone -->
    <Geometry x:Key="GeoPhone">M22,16.92 V19.92 A2,2 0 0,1 19.82,22 A19.79,19.79 0 0,1 11.19,18.93 A19.5,19.5 0 0,1 5.19,12.93 A19.79,19.79 0 0,1 2.12,4.26 A2,2 0 0,1 4.11,2.26 H7.11 A2,2 0 0,1 9.11,3.98 C9.33,4.98 9.68,5.95 10.14,6.87 A2,2 0 0,1 9.69,8.98 L8.42,10.25 C9.86,12.78 11.92,14.84 14.45,16.28 L15.72,15.01 A2,2 0 0,1 17.83,14.56 C18.75,15.02 19.72,15.37 20.72,15.59 A2,2 0 0,1 22,16.92 Z</Geometry>
    <!-- map-pin -->
    <Geometry x:Key="GeoMapPin">M20,10 C20,16 12,22 12,22 C12,22 4,16 4,10 A8,8 0 0,1 20,10 Z M12,13 A3,3 0 1,0 12,7 A3,3 0 0,0 12,13 Z</Geometry>
    <!-- edit-3 -->
    <Geometry x:Key="GeoEdit3">M12,20 H21 M16.5,3.5 A2.12,2.12 0 0,1 19.5,6.5 L7,19 L3,20 L4,16 L16.5,3.5 Z</Geometry>
    <!-- help-circle -->
    <Geometry x:Key="GeoHelp">M12,22 A10,10 0 1,0 12,2 A10,10 0 0,0 12,22 Z M9.09,9 A3,3 0 0,1 14.92,10 C14.92,12 12,13 12,13 M12,17 H12.01</Geometry>
    <!-- sun -->
    <Geometry x:Key="GeoSun">M12,1 V3 M12,21 V23 M4.22,4.22 L5.64,5.64 M18.36,18.36 L19.78,19.78 M1,12 H3 M21,12 H23 M4.22,19.78 L5.64,18.36 M18.36,5.64 L19.78,4.22 M12,17 A5,5 0 1,0 12,7 A5,5 0 0,0 12,17 Z</Geometry>
    <!-- grid-3x3 -->
    <Geometry x:Key="GeoGrid">M3,3 H9 V9 H3 Z M15,3 H21 V9 H15 Z M3,15 H9 V21 H3 Z M15,15 H21 V21 H15 Z</Geometry>
    <!-- save -->
    <Geometry x:Key="GeoSave">M19,21 H5 A2,2 0 0,1 3,19 V5 A2,2 0 0,1 5,3 H16 L21,8 V19 A2,2 0 0,1 19,21 Z M17,21 V13 H7 V21 M7,3 V8 H15</Geometry>
    <!-- trash-2 -->
    <Geometry x:Key="GeoTrash">M3,6 H21 M19,6 V20 A2,2 0 0,1 17,22 H7 A2,2 0 0,1 5,20 V6 M8,6 V4 A2,2 0 0,1 10,2 H14 A2,2 0 0,1 16,4 V6 M10,11 V17 M14,11 V17</Geometry>

    <!-- =========================================================================
         TOPBAR & BUTTON STYLES
         ========================================================================= -->

    <!-- Topbar Icon Button Style (28x28 subtle hover) -->
    <Style x:Key="TopbarIconBtn" TargetType="{x:Type Button}">
        <Setter Property="Width" Value="28" />
        <Setter Property="Height" Value="28" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Foreground" Value="{StaticResource BrushTextMuted}" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border x:Name="bd" Background="{TemplateBinding Background}" CornerRadius="6">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="{StaticResource BrushSurface2}" />
                            <Setter Property="Foreground" Value="{StaticResource BrushTextPrimary}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="{StaticResource BrushSurface3}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Global Universal Search TextBox Style -->
    <Style x:Key="SearchTextBox" TargetType="{x:Type TextBox}">
        <Setter Property="Background" Value="#121212" />
        <Setter Property="Foreground" Value="{StaticResource BrushTextPrimary}" />
        <Setter Property="BorderBrush" Value="{StaticResource BrushBorder}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="32,6,74,6" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="CaretBrush" Value="{StaticResource BrushAccentSky}" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TextBox}">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="6"
                            SnapsToDevicePixels="True">
                        <Grid>
                            <!-- Search Icon (Left) -->
                            <Path Data="{StaticResource GeoSearch}"
                                  Stroke="{StaticResource BrushTextMuted}"
                                  StrokeThickness="1.6"
                                  Width="14" Height="14"
                                  HorizontalAlignment="Left"
                                  VerticalAlignment="Center"
                                  Margin="10,0,0,0"
                                  IsHitTestVisible="False" />

                            <ScrollViewer x:Name="PART_ContentHost" Focusable="False" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden" />

                            <!-- Watermark / Placeholder -->
                            <TextBlock x:Name="watermark"
                                       Text="{TemplateBinding Tag}"
                                       Foreground="{StaticResource BrushTextMuted}"
                                       FontSize="{TemplateBinding FontSize}"
                                       Margin="32,0,0,0"
                                       VerticalAlignment="Center"
                                       IsHitTestVisible="False"
                                       Visibility="Collapsed" />
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="Text" Value="" />
                                <Condition Property="IsKeyboardFocused" Value="False" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="watermark" Property="Visibility" Value="Visible" />
                        </MultiTrigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource BrushBorderStrong}" />
                        </Trigger>
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource BrushAccentSky}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Blue Find / Accent Button Style -->
    <Style x:Key="AccentButton" TargetType="{x:Type Button}">
        <Setter Property="Background" Value="{StaticResource BrushAccentSkyDark}" />
        <Setter Property="Foreground" Value="#ffffff" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="12,5" />
        <Setter Property="FontSize" Value="11" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border x:Name="bd" Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="{StaticResource BrushAccentSky}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="#0284c7" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Standard Modern Button Style -->
    <Style x:Key="ModernButton" TargetType="{x:Type Button}">
        <Setter Property="Background" Value="{StaticResource BrushSurface1}" />
        <Setter Property="Foreground" Value="{StaticResource BrushTextPrimary}" />
        <Setter Property="BorderBrush" Value="{StaticResource BrushBorder}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="12,6" />
        <Setter Property="FontSize" Value="11.5" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border x:Name="bd"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="6"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="{StaticResource BrushSurface2}" />
                            <Setter TargetName="bd" Property="BorderBrush" Value="{StaticResource BrushBorderStrong}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="{StaticResource BrushSurface0}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Standard Modern Form TextBox Style -->
    <Style x:Key="ModernTextBox" TargetType="{x:Type TextBox}">
        <Setter Property="Background" Value="#121212" />
        <Setter Property="Foreground" Value="{StaticResource BrushTextPrimary}" />
        <Setter Property="BorderBrush" Value="{StaticResource BrushBorder}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="8,5" />
        <Setter Property="FontSize" Value="11.5" />
        <Setter Property="CaretBrush" Value="{StaticResource BrushAccentSky}" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TextBox}">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="4"
                            SnapsToDevicePixels="True">
                        <Grid>
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="False" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
                            <TextBlock x:Name="watermark"
                                       Text="{TemplateBinding Tag}"
                                       Foreground="{StaticResource BrushTextMuted}"
                                       FontSize="{TemplateBinding FontSize}"
                                       Margin="8,0,0,0"
                                       VerticalAlignment="Center"
                                       IsHitTestVisible="False"
                                       Visibility="Collapsed" />
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="Text" Value="" />
                                <Condition Property="IsKeyboardFocused" Value="False" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="watermark" Property="Visibility" Value="Visible" />
                        </MultiTrigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource BrushBorderStrong}" />
                        </Trigger>
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource BrushAccentSky}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- =========================================================================
         LEFT ICON RAIL NAVIGATION RADIOBUTTON (.nav-item)
         ========================================================================= -->
    <Style x:Key="RailNavItem" TargetType="{x:Type RadioButton}">
        <Setter Property="GroupName" Value="SidebarNav" />
        <Setter Property="Height" Value="36" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{StaticResource BrushTextMuted}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type RadioButton}">
                    <Border x:Name="bd"
                            Background="{TemplateBinding Background}"
                            CornerRadius="6"
                            Margin="2,1"
                            Padding="8,0">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <!-- Icon Column (26px) -->
                                <ColumnDefinition Width="26" />
                                <!-- Label Column (fills remaining width) -->
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <!-- Left Blue Accent Bar (visible only when checked) -->
                            <Rectangle x:Name="accentBar"
                                       Width="2.5"
                                       Height="20"
                                       RadiusX="1" RadiusY="1"
                                       Fill="{StaticResource BrushAccentSky}"
                                       HorizontalAlignment="Left"
                                       VerticalAlignment="Center"
                                       Margin="-6,0,0,0"
                                       Visibility="Collapsed" />

                            <!-- Icon -->
                            <ContentPresenter Grid.Column="0"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />

                            <!-- Text Label -->
                            <TextBlock x:Name="txtLabel"
                                       Grid.Column="1"
                                       Text="{TemplateBinding Tag}"
                                       Foreground="{TemplateBinding Foreground}"
                                       FontSize="11.5"
                                       FontWeight="Medium"
                                       VerticalAlignment="Center"
                                       Margin="8,0,0,0"
                                       TextTrimming="CharacterEllipsis" />
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="{StaticResource BrushSurface2}" />
                            <Setter Property="Foreground" Value="{StaticResource BrushTextPrimary}" />
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="{StaticResource BrushSurface3}" />
                            <Setter TargetName="accentBar" Property="Visibility" Value="Visible" />
                            <Setter Property="Foreground" Value="#ffffff" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- =========================================================================
         FLOATING CANVAS CONTROLS & CAPSULES
         ========================================================================= -->

    <!-- Floating Capsule Toolbar Button -->
    <Style x:Key="CapsuleButton" TargetType="{x:Type Button}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{StaticResource BrushTextSecondary}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Width" Value="28" />
        <Setter Property="Height" Value="28" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border x:Name="bd" Background="{TemplateBinding Background}" CornerRadius="6">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="{StaticResource BrushSurface2}" />
                            <Setter Property="Foreground" Value="#ffffff" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="{StaticResource BrushSurface3}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Floating Circular Prev/Next Button -->
    <Style x:Key="CanvasCircleNavButton" TargetType="{x:Type Button}">
        <Setter Property="Width" Value="46" />
        <Setter Property="Height" Value="46" />
        <Setter Property="Background" Value="#CC181818" />
        <Setter Property="BorderBrush" Value="{StaticResource BrushBorderStrong}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Foreground" Value="{StaticResource BrushTextSecondary}" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border x:Name="bd"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="23">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="16" ShadowDepth="2" Opacity="0.45" Color="#000000" />
                        </Border.Effect>
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="#EE252525" />
                            <Setter TargetName="bd" Property="BorderBrush" Value="{StaticResource BrushAccentSky}" />
                            <Setter Property="Foreground" Value="{StaticResource BrushAccentSky}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="#FF141414" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Copy Small Icon Button (Inside Consumer Details Card) -->
    <Style x:Key="CopySmallButton" TargetType="{x:Type Button}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Width" Value="18" />
        <Setter Property="Height" Value="18" />
        <Setter Property="Foreground" Value="{StaticResource BrushTextMuted}" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border x:Name="bd" Background="{TemplateBinding Background}" CornerRadius="3">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="{StaticResource BrushSurfaceHover}" />
                            <Setter Property="Foreground" Value="{StaticResource BrushTextPrimary}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Slim Modern ScrollViewer / ScrollBar -->
    <Style x:Key="ScrollBarThumb" TargetType="{x:Type Thumb}">
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="OverridesDefaultStyle" Value="true"/>
        <Setter Property="IsTabStop" Value="false"/>
        <Setter Property="Focusable" Value="false"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Thumb}">
                    <Border CornerRadius="3.5" Background="#40ffffff" Margin="1" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type ScrollBar}">
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="OverridesDefaultStyle" Value="true"/>
        <Style.Triggers>
            <Trigger Property="Orientation" Value="Vertical">
                <Setter Property="Width" Value="7"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type ScrollBar}">
                            <Grid Background="Transparent">
                                <Track x:Name="PART_Track" IsDirectionReversed="true">
                                    <Track.Thumb>
                                        <Thumb Style="{StaticResource ScrollBarThumb}"/>
                                    </Track.Thumb>
                                </Track>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Trigger>
            <Trigger Property="Orientation" Value="Horizontal">
                <Setter Property="Height" Value="7"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type ScrollBar}">
                            <Grid Background="Transparent">
                                <Track x:Name="PART_Track">
                                    <Track.Thumb>
                                        <Thumb Style="{StaticResource ScrollBarThumb}"/>
                                    </Track.Thumb>
                                </Track>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Trigger>
        </Style.Triggers>
    </Style>

</ResourceDictionary>
